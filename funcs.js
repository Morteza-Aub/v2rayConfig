// Test Codes

const likePost = () => {
  return true;
};

const disslikePost = () => {
  return false;
};

const getMe = (user) => {
  return user.name;
};

const setComment = (comment) => {
  return comment.body;
};
